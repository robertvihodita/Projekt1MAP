# Existing Thymeleaf configuration
spring.thymeleaf.prefix=classpath:/

# NEW: MySQL Database Configuration [cite: 37, 38, 39]
# 
# ?? REMEMBER TO REPLACE THE VALUES BELOW WITH YOUR ACTUAL CONFIGURATION!
#
spring.datasource.url=jdbc:mysql://localhost:8080/Projekt1MAP?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# NEW: Hibernate/JPA Properties
# 'update' attempts to create tables if they don't exist and update schemas.
# Set to 'none' for production or 'create-drop' for quick testing.
spring.jpa.hibernate.ddl-auto=update

# Show SQL in the console (helpful for debugging)
spring.jpa.show-sql=true